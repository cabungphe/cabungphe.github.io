services:
  ezhik_task:
    build: .
    ports:
      - 5364:5364
    restart: unless-stopped
    environment:
      - FLAG=flag{test}
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
